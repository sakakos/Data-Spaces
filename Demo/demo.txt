$PROVIDER_TOKEN = docker run --rm -v "C:\Users\SAKak\Documents\GitHub\Data-Spaces\Kong\iShare:/iShare" python:3.9-slim sh -c "pip install PyJWT cryptography -q 2>/dev/null && python3 /iShare/gen_token.py"

$LINK = '<http://context:3004/user-context.jsonld>; rel="http://www.w3.org/ns/json-ld#context"; type="application/ld+json"'

# 1. Χωρίς token - πρέπει 401
Write-Host "=== Χωρίς token ==="
curl.exe -s "http://localhost:8000/ngsi-ld/v1/entities?type=HouseholdElectricMeasurement&limit=1"

# 2. Provider token με Link header - πρέπει να επιστρέψει entities
Write-Host "`n=== Provider token ==="
curl.exe -s -H "Authorization: Bearer $PROVIDER_TOKEN" -H "Link: $LINK" "http://localhost:8000/ngsi-ld/v1/entities?type=HouseholdElectricMeasurement&limit=1"

# 3. Consumer token
Write-Host "`n=== Consumer token ==="
curl.exe -s -H "Authorization: Bearer $ACCESS_TOKEN" -H "Link: $LINK" "http://localhost:8000/ngsi-ld/v1/entities?type=HouseholdElectricMeasurement&limit=1"